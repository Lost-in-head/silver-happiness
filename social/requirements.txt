# Core LLM stack — OpenRouter uses the OpenAI-compatible API
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# Local embeddings — no API key, downloads ~80MB model on first use
sentence-transformers>=3.0.0

# Vector store
chromadb>=0.5.0

# Document loading for teach command
pypdf>=5.0.0
unstructured>=0.14.0

# Config and env
python-dotenv>=1.0.0
pyyaml>=6.0

# CLI
click>=8.0.0

# Browser automation (Reddit adapter)
playwright>=1.49.0

# Scheduler
schedule>=1.2.0

# Web API (serve.py / src/api/)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Testing
pytest>=8.0.0
pytest-mock>=3.14.0
httpx>=0.27.0
